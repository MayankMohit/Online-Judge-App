{
  "name": "online-judge-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend,compiler -c blue,magenta,yellow \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"npm --prefix compiler-service run dev\"",
    "install-all": "npm install && npm --prefix backend install && npm --prefix frontend install && npm --prefix compiler-service install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
